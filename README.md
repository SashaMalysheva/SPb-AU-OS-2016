# SPb-AU-OS-2016
CoverLetter:



1. В Makefile добавлены новые зависимости.

2. В allocator.h и slab.h новые зависимости, так как в allocator.c и slab.c теперь работа с памятью через критическую секцию.

3. lock.* — первое задание.

4. В main.c появилась инициализация пула потоков и вызов тестов (один из тестов закомментирован, 
так как последние два одновременно не проверяются).

5. В pit.h новая зависимость, так как pit.c по таймеру переключает потоки.

6. test.* — тесты.

7. thread.* и thread_handlers.S — второе и третье задания.